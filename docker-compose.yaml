version: '3.8'

services:
  feedback:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: feedback
    restart: always
    ports:
      - "8080:8080"
    environment:
      - APOLLO_SECRET=${APOLLO_SECRET}
    networks:
      - feedback-network


networks:
  feedback-network:
    driver: bridge